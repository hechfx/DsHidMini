name: build project

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
        - name: build with msbuild
          run: |
            cd build
            dotnet msbuild _build.csproj
