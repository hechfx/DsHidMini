name: build project

on: [ push ]

jobs:
  build:
    runs-on: self-hosted

    steps:
        - uses: microsoft/setup-msbuild@v1.3.1
          with:
            msbuild-architecture: x64

        - name: build with msbuild
          run: |
            cd build
            msbuild _build.csproj
